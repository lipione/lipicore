# Architecture

The canonical architecture document is [docs/architecture.md](docs/architecture.md).

Keep this root file as a short pointer so architecture details do not drift across duplicate documents.
